from setuptools import setup, findpackages

setup(
    name="isdetranslator",
    version="0.0.1",
    author="Adam Leadbetter",
    platforms="any",
    packages=find_packages(include=['isdetranslator'])
)